<div class="menu">
  <nav class="navbar navbar-default navbar-fixed-top">
    <div class="container-fluid">
      <div class="navbar-header hamburger">
        <button type="button"
          class="hamburger__button navbar-toggle collapsed"
          data-toggle="collapse"
          data-target="#bs-example-navbar-collapse-1"
          aria-expanded="false" >
          <span class="sr-only">Toggle navigation</span>

          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <i class="glyphicon glyphicon-remove"></i>
        </button>
      </div>

      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

        <ul class="nav navbar-nav navbar-left js_topMenu">
          <li>
            <%= link_to root_url do %>
              Home <span class="sr-only">(current)</span></a>
            <% end %>
          </li>
          <li><%= link_to "Catálogo", catalog_view_path %></li>
          <li><%= link_to '¿Cómo funciona?', page_path('tutorial') %></li>
          <li><%= link_to 'Contacto', new_contact_path %></li>
        </ul>

        <ul class="nav navbar-nav navbar-right">

          <li>
            <a href="#searchModal" role="button" class="search_btn" data-toggle="modal" id="search_path">
              <i class="glyphicon glyphicon-search"></i> Buscar Producto
            </a>
          </li>

          <%  if user_signed_in?  %>
          <li class="dropdown" >
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" id="store_path">
              <i class="fa fa-user" aria-hidden="true"></i> Opciones
            </a>
            <% if current_user.type.eql?("Store") and current_user.pilot? %>
              <%= render 'helpers/store_menu' %>
            </li>
            <% elsif current_user.type == "Customer" %>
              <%= render 'helpers/customer_menu' %>
            <% else %>
              <%= render 'helpers/suscriber_menu' %>
            <% end %>
          <% else %>
            <li>
              <a href="#login" role="button" data-toggle="modal" data-target="#loginModal">
                <i class="glyphicon glyphicon-user"></i> Comenzar
              </a>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
</div>
