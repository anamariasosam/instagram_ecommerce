<header class="sectionHero">
  <div class="container">
    <h2>Lista de Espera</h2>
  </div>
</header>
<section class="container">
  <% if current_user.pilot? %>
    <p>
      ¡La espera terminó!
    </p>
    <%= link_to "Comenzar", users_dashboard_path %>
  <% else %>
  <p>
    Estás a punto de comenzar la mejor experiencia de venta a través de Instagram. Actualmente te encuentras en lista de espera.
    Turno de espera: <%= current_user.id %>
    Total de usuarios en espera: <%= @waiting_users %>
  </p>
  <% end %>
</section>
