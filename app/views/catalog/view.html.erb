<% content_for :title, "Catálogo - " %>

<% content_for :mobile_header do %>
  <h3 class="mobileHeader__headline">Catálogo</h3>
<% end %>

<% content_for :headline do %>
  <h3 class="sectionHero__headline">Catálogo</h3>
<% end %>

<section class="container">

    <header class="major">
      <%= render 'categories/list' %>
    </header>

    <div class="row">
      <%= render @products %>
    </div>

    <div class="paginator text-center">
      <%= paginate @products %>
    </div>

</section>
